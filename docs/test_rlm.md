# Goal: Test RLM Confusion Detection

1. Create a file 'confusion.py' with the content 'confused = True'.
2. Run a command that doesn't exist: 'trigger-error-now'
3. Write the exact same content to 'confusion.py' again.
4. Try to read a file that doesn't exist: 'missing_file.txt'
